{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum desktop permissions for the Utu shell.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "window-state:default"
  ]
}
