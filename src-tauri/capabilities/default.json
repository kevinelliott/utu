{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum desktop permissions for the Utu shell.",
  "windows": ["main", "about"],
  "permissions": [
    "core:default",
    "core:event:default",
    "dialog:allow-open"
  ]
}
